/* Curriculum tasks use real school apps. Returning here never proves mastery. */
const ipadAssignments = {
 systemen: {title:'Maak een raadsel',goals:['IT.002','IT.003','IT.004','IT.014'],
  setup:'Leg een voorwerp klaar. Voorzie Camera, Foto’s en Dictafoon of de opname-app van de school. Kies een voorwerp zonder mensen in beeld.',
  brief:'Laat je maatje raden welk voorwerp jij koos. Kies een foto of een geluidsraadsel. Maak je raadsel en toon of speel het af.',
  steps:['Kies wat jouw maatje moet zien of horen. Welke app helpt daarbij?','Open de echte Camera of opname-app. Maak je raadsel.','Open je foto in Foto’s, of luister je opname terug. Is je raadsel bruikbaar? Probeer zo nodig opnieuw.','Laat je maatje raden. Vertel wat jij aan de iPad gaf, wat de app ermee deed en wat je maatje ziet of hoort.'],
  extra:'Maak daarna een ander raadsel met de andere app. Wat blijft hetzelfde aan invoer, verwerking en uitvoer?',
  criteria:'Kind kiest een passende app, maakt en controleert een eigen resultaat en koppelt invoer, verwerking en uitvoer aan dat resultaat. Bespreek wanneer het echte voorwerp tonen eenvoudiger is. Voor IT.002: bekijk met 6–7-jarigen een toegelaten deelroute met internet; lokaal tonen is geen contact op afstand.'},
 bedienen: {title:'Kijken wanneer jij dat wilt',goals:['IT.027','IT.035','IT.042'],
  setup:'Zet een kort filmpje klaar in de schoolapp of Foto’s. Controleer het geluid vooraf. Toon waar de volumeknoppen op deze iPad zitten.',
  brief:'Toon een filmpje aan je maatje. Zet het stil bij iets interessants. Vertel wat je ziet. Kijk daarna samen verder.',
  steps:['Open de app en start het filmpje.','Gebruik de echte volumeknoppen. Kies een rustig, goed hoorbaar geluid.','Pauzeer bij iets wat je wilt tonen. Vertel erover. Speel verder.','Ga naar het beginscherm. Open de app opnieuw. Wijs ook het batterijpictogram aan.'],
  extra:'Laat je maatje vragen om stiller te zetten of even te pauzeren. Reageer zelf op die vraag.',
  criteria:'Observeer openen, afspelen, functioneel pauzeren, hervatten, passend geluid en batterij herkennen. Teruggaan naar het beginscherm is de app verlaten, niet geforceerd afsluiten. Een kruisje in de oefenapp bewijst deze toestelbediening niet.'},
 bestanden: {title:'Bewaar iets dat je terugvindt',goals:['IT.028','IT.029','IT.030','IT.035'],
  setup:'Maak in Bestanden een aparte oefenmap met kopieën van een tekst, afbeelding, geluid en video, plus een wegwerpbestand proefklad. Kies een schoolapp waarmee een eigen werkje in die map kan worden bewaard. Laat een programma in de oefenmissie openen en gebruiken.',
  brief:'Bewaar een eigen werkje in de oefenmap. Geef het een naam waaraan je het morgen herkent. Open het opnieuw. Ruim alleen proefklad op.',
  steps:['Maak een kort bericht of een tekening in de afgesproken app.','Kies zelf een herkenbare naam. Bewaar via de bewaar- of deelknop in de oefenmap die je leerkracht aanwijst.','Verlaat je werkje. Open Bestanden en zoek je werkje terug. Open het en controleer de inhoud.','Open de andere oefenbestanden. Wat is tekst, beeld, geluid of video? Speel geluid en video ook af.','Verwijder alleen de oefenkopie proefklad. Controleer dat jouw eigen werkje er nog staat.'],
  extra:'Leg uit waarom jouw bestandsnaam helpt om je werk terug te vinden.',
  criteria:'Kind opent om het type te bepalen, benoemt herkenbaar, bewaart, verlaat, heropent en verwijdert gericht. Observeer de echte oefenmap. Beoordeel programma herkennen daarnaast in de interactieve bestandsopdracht. Verwijder geen persoonlijke bestanden.'},
 toetsenbord: {title:'Een echt bericht voor je maatje',goals:['IT.037','IT.042','IT.044'],
  setup:'Gebruik Notities of de schrijfapp van de school. Zet voor deze oefening automatische correctie en hoofdletters zo nodig samen uit, of noteer welke hulp ze geven.',
  brief:'Schrijf een kort bericht voor je maatje. Vertel wat je wilt spelen en met hoeveel kinderen. Stel ook een vraag.',
  steps:['Open een nieuw bericht in de schrijfapp. Tik om het echte toetsenbord te openen.','Schrijf je eigen woorden. Gebruik een cijfer, spaties en een punt.','Stel een vraag met een vraagteken. Schrijf ook een uitroep met een uitroepteken.','Lees je bericht terug. Zet de cursor bij een fout, wis die en verbeter. Laat je maatje het lezen.','Leg de iPad en koptelefoon rustig terug.'],
  extra:'Verbeter een woord midden in je bericht zonder de hele zin opnieuw te typen.',
  criteria:'Observeer letters, cijfers, ! ? . en functioneel gebruik van het virtuele toetsenbord. Spaties, hoofdletters en cursorcorrectie ondersteunen de opdracht, maar zijn geen extra afzonderlijke leerplannormen. Bij 6–7 mag het bericht samen worden opgebouwd; noteer gegeven hulp.'},
 zoeken: {title:'Zoek een antwoord voor de klas',goals:['IT.045','IT.046','IT.055','IT.058'],
  setup:'Open vooraf een door de school gekozen zoektool met passende resultaten, bijvoorbeeld Schooltv. Controleer de beschikbare bronnen. Geef de zoekwoorden bijen honing; zelfstandig zoekwoorden bedenken is hier geen eis.',
  brief:'Hoe maken bijen honing? Zoek met de woorden bijen honing. Toon een bron die je helpt om het aan je maatje uit te leggen.',
  steps:['Open de zoektool die je leerkracht klaarzette. Typ bijen honing in het zoekvak.','Open een resultaat en lees of bekijk de inhoud. Je mag laten voorlezen.','Helpt deze bron om de vraag te beantwoorden? Bekijk ook een ander resultaat.','Toon de bruikbare bron en vertel welk stukje het antwoord geeft.'],
  extra:'Vertel waarom het andere resultaat je minder goed helpt. Een bron over honingkoekjes hoeft niet uit te leggen hoe bijen honing maken.',
  criteria:'Kind voert aangereikte zoektermen in, opent bronnen, herkent bruikbaarheid en verantwoordt de keuze. Observeer het zoeken zelf; een juist feit of een geopende link bewijst dit niet. Bij 6–7 ook laten uitleggen waarvoor de zoektool helpt.'},
 mediafit: {title:'Onze schermafspraak',goals:['IT.061','IT.064','IT.044'],
  setup:'Werk met een maatje. Spreek een kort werkmoment en een stopsignaal af. Begeleid het gesprek zonder persoonlijke ervaringen als goed of fout te beoordelen.',
  brief:'Maak samen een afspraak waardoor jullie fijn kunnen werken. Probeer de afspraak tijdens jullie volgende opdracht.',
  steps:['Vertel over een schermmoment dat fijn of juist onrustig voelde. Luister naar je maatje.','Kies samen één afspraak: om de beurt, rustig geluid, een pauze of stoppen op het signaal.','Doe een echte opdracht en pas jullie afspraak toe.','Stop op het signaal. Leg het toestel neer en beweeg of kijk even weg.','Bespreek: hielp de afspraak jou en je maatje? Wat willen jullie volgende keer doen?'],
  extra:'Leg uit hoe jouw schermgebruik ook invloed heeft op je maatje.',
  criteria:'Observeer de afspraak tijdens werkelijk gebruik en opnieuw op een ander moment. Een timer, aanklikken van een gevoel of verderknop bewijst geen gezond gedrag.'},
 creatie: {title:'Nodig iemand uit',goals:['IT.092','IT.093','IT.042'],
  setup:'Voorzie een teken-, schrijf- en opname-app die de school gebruikt. De ontvanger is een klasgenoot; delen buiten de klas is niet nodig.',
  brief:'Nodig je maatje uit voor een spel. Kies zelf hoe: een kaart, een tekening met woorden of een luisterbericht. Maak iets dat je maatje begrijpt.',
  steps:['Vertel wat je maatje moet weten. Welke app kan dat?','Open jouw gekozen app en maak de uitnodiging.','Lees, bekijk of beluister je werk zelf. Is duidelijk welk spel je bedoelt?','Laat je maatje reageren. Verbeter iets als dat nodig is.','Vertel wat de app kan en waarom jij die koos.'],
  extra:'Maak voor een maatje dat nog niet goed leest een andere versie. Kies welke tool daarbij helpt.',
  criteria:'De toolkeuze leidt tot werkelijk maken, controleren en waar nodig verbeteren. Observeer de inhoud en de uitleg over de mogelijkheden van de gekozen app; twee kleuren of een bewaarklik volstaan niet.'}
};

// Transfer tasks remain observation tasks, including after reloading the page.
Object.entries(ipadAssignments).forEach(([id,assignment])=>{
 const m=missions.find(m=>m.id===id);
 m.tasks.push({...assignment,id:'ipad-transfer-v1-'+id,type:'ipad-transfer',prompt:assignment.brief,requiresObservation:true});
 m.goals=[...new Set(m.tasks.flatMap(t=>t.goals))];
});

function showIPadAssignment(task){
 const a=$('#taskArea');
 a.innerHTML=`<section class="work-panel ipad-assignment"><span class="eyebrow">OP JE ECHTE IPAD</span><h3>${escapeHtml(task.title)}</h3><p>${escapeHtml(task.brief)}</p>${sayButton(task.brief)}<p class="ipad-return">Voer de opdracht uit in de echte app. Kom daarna terug naar Zisa.</p><details><summary>Help mij op weg</summary><ol>${task.steps.map(s=>`<li>${escapeHtml(s)} ${sayButton(s)}</li>`).join('')}</ol></details>${state.age==='7-8'?`<details><summary>Een stap verder</summary><p>${escapeHtml(task.extra)}</p>${sayButton(task.extra)}</details>`:''}<details><summary>Voorbereiding en observatie voor de leerkracht</summary><p>${escapeHtml(task.setup)}</p><p>${escapeHtml(task.criteria)}</p><p>Help waar nodig. Noteer achteraf hoeveel hulp het kind kreeg. De extra uitdaging is een didactische keuze, geen bijkomende leerplannorm.</p></details><button data-return class="primary-button">Ik ben terug</button><p class="practice-note" role="status"></p></section>`;
 wireRead();
 a.querySelector('[data-return]').onclick=()=>{
  a.querySelector('[data-return]').disabled=true;
  session.answered=true;session.correct++;
  note('Vertel of toon wat je deed. Je leerkracht noteert wat al lukt en wat je nog oefent.');
  const next=document.createElement('button');next.className='primary-button';next.textContent='Verder';
  next.onclick=()=>{session.index++;renderTask()};a.append(next);
 };
}
const renderCurriculumTask=renderTask;
renderTask=function(){
 const task=session?.tasks[session.index];
 if(task?.type!=='ipad-transfer')return renderCurriculumTask();
 cleanupPractice();cleanupPractice=()=>{};window.speechSynthesis?.cancel();
 document.querySelectorAll('video,audio').forEach(el=>el.pause());session.answered=false;
 body.innerHTML='<div id="taskArea"></div>';showIPadAssignment(task);
};
// Keep the existing printable cards and make the real assignments readable on iPad.
const startCurriculumMission=startMission;
startMission=function(id){
 if(id!=='systemen')return startCurriculumMission(id);
 const mission=missions.find(m=>m.id===id),task=mission.tasks.find(t=>t.type==='ipad-transfer');
 session={mission,tasks:[task],index:0,correct:0,answered:false};
 $('#missionTitle').textContent=mission.title;$('#missionNumber').textContent='Samen doen · '+state.age+' jaar';
 renderTask();
 const link=document.createElement('a');link.href='assets/zisas-ipad-opdrachtkaarten.pdf';link.target='_blank';link.rel='noopener';link.className='text-button';link.textContent='Ook beschikbaar: de 8 afdrukbare iPad-kaartjes';body.append(link);dialog.showModal();
};

// A debugging task is an extension of a linear route, not a new curriculum norm.
const debugRoute=R('Dit plan bevat een fout. Test het. Verander daarna een pijl zodat de robot de ster bereikt.',4,3,[0,1],[3,1],['↑','→','→','→','↓'],['IT.015','IT.019'],[[1,1]]);
Object.assign(debugRoute,{id:'robot-debug-v1',age:'7-8',initial:['↑','→','↓','→','↓']});
robotMission.tasks.push(debugRoute);
robotMission.tasks[2].prompt='Breng de robot naar de ster. Zoek een kort stukje met pijlen dat je twee keer kunt gebruiken. Test je plan.';

function runRoute(task,arrows){
 let [x,y]=task.start;const path=[[x,y]];
 for(let i=0;i<arrows.length;i++){
  const delta={'↑':[0,-1],'↓':[0,1],'→':[1,0],'←':[-1,0]}[arrows[i]];
  if(!delta)return {path,ok:false,failed:i};
  x+=delta[0];y+=delta[1];
  if(x<0||y<0||x>=task.cols||y>=task.rows||task.rocks.some(([a,b])=>a===x&&b===y))return {path,ok:false,failed:i};
  path.push([x,y]);
 }
 return {path,ok:x===task.finish[0]&&y===task.finish[1],failed:-1};
}
renderRoute=function(task){
 const a=$('#taskArea'),entered=[...(task.initial||[])];let selected=-1,busy=false,alive=true;
 const cells=[];for(let y=0;y<task.rows;y++)for(let x=0;x<task.cols;x++)cells.push(`<div class="route-cell" data-x="${x}" data-y="${y}">${task.rocks.some(([rx,ry])=>rx===x&&ry===y)?'🪨':x===task.finish[0]&&y===task.finish[1]?'★':''}</div>`);
 a.innerHTML=`<section class="work-panel route-work"><p>Elke pijl is één vakje. De pijlen wijzen naar de rand van het scherm.</p><div class="route-board" style="--route-cols:${task.cols};grid-template-columns:repeat(${task.cols},58px)">${cells.join('')}</div><div class="route-code" aria-label="Jouw stappenplan"></div><p>Kies een stap om die te veranderen. Zonder gekozen stap voeg je achteraan toe.</p><div class="arrow-pad">${['↑','→','↓','←'].map((p,i)=>`<button data-arrow="${p}" aria-label="${['Omhoog','Rechts','Omlaag','Links'][i]}">${p}</button>`).join('')}<button data-end>+ Achteraan</button><button data-delete>Wis stap</button></div><button data-run class="primary-button">Test mijn plan</button><details><summary>Denkhulp</summary><p>Waar moet je heen? Welke vakjes zijn belangrijk? Verdeel de weg in kleine stukjes. Komt een stukje terug?</p>${sayButton('Verdeel de weg in kleine stukjes. Komt een stukje terug?')}</details><p class="practice-note" role="status"></p></section>`;
 const token=document.createElement('img');token.src='assets/robot-zisa.png';token.alt='Robot';token.className='robot-token';
 const place=([x,y])=>a.querySelector(`[data-x="${x}"][data-y="${y}"]`).append(token);place(task.start);
 const draw=()=>{
  a.querySelector('.route-code').innerHTML=entered.length?entered.map((p,i)=>`<button data-step="${i}" aria-label="Stap ${i+1}: ${p}" aria-pressed="${selected===i}"><small>${i+1}</small>${p}</button>`).join(''):'<span>Maak je stappenplan.</span>';
  a.querySelectorAll('[data-step]').forEach(b=>b.onclick=()=>{if(busy||session.answered)return;selected=Number(b.dataset.step);draw()});
 };
 a.querySelectorAll('[data-arrow]').forEach(b=>b.onclick=()=>{if(busy||session.answered)return;if(selected>=0){entered[selected]=b.dataset.arrow;selected=-1}else if(entered.length<32)entered.push(b.dataset.arrow);draw()});
 a.querySelector('[data-end]').onclick=()=>{if(busy)return;selected=-1;draw()};
 a.querySelector('[data-delete]').onclick=()=>{if(busy||session.answered)return;entered.splice(selected>=0?selected:entered.length-1,1);selected=-1;draw()};
 a.querySelector('[data-run]').onclick=async()=>{
  if(busy||session.answered||!entered.length)return;busy=true;place(task.start);
  a.querySelectorAll('button').forEach(b=>b.disabled=true);
  const result=runRoute(task,entered);
  for(let i=1;i<result.path.length;i++){await new Promise(r=>setTimeout(r,350));if(!alive)return;place(result.path[i]);a.querySelectorAll('[data-step]').forEach((b,j)=>b.classList.toggle('running',j===i-1))}
  if(!alive)return;busy=false;a.querySelectorAll('button').forEach(b=>b.disabled=false);
  if(result.ok)completePractice(task,'Je plan werkt. Leg aan je maatje uit welke kleine stukjes je gebruikte.');
  else{selected=result.failed;draw();note(result.failed>=0?`Bij stap ${result.failed+1} botst de robot. Je plan blijft staan. Verander een stap en test opnieuw.`:'De robot is nog niet bij de ster. Je plan blijft staan. Verander het of voeg stappen toe.');}
 };draw();wireRead();cleanupPractice=()=>{alive=false};
};

// Record observations per learner code; device-wide exercise hits stay explicitly separate.
const observationKey='zisa-curriculum-observations-v1';
let observationLearner='';
function readObservations(){const data=readJson(observationKey,[]);return Array.isArray(data)?data:[]}
const levels={'not-yet':'Nog oefenen',help:'Met hulp',independent:'Zelfstandig'};
openTeacher=function(){
 const t=$('#teacherDialog');if(t.open)t.close();
 if(!state.age){$('#teacherSummary').textContent='Kies eerst een leeftijd op het startscherm.';$('#teacherGoals').innerHTML='';t.showModal();return;}
 const goals=curriculumSource.filter(g=>g.ages.includes(state.age));
 $('#teacherSummary').textContent=`${state.age} jaar · ${goals.length} leerplandoelen. Laat een groepje oefenen en kijk mee: lukt het zelfstandig, met hulp of nog niet? Inloggen en digitaal registreren zijn niet nodig. Open hieronder de opdrachten en aandachtspunten. Een missiester is geen beheersingsbewijs.`;
 $('#teacherGoals').innerHTML=`<details class="observation-settings"><summary>Optioneel: observaties digitaal bijhouden</summary><label>Eigen leerlingcode (bijvoorbeeld L01, geen account)<input id="observationLearner" maxlength="40" autocomplete="off" value="${escapeHtml(observationLearner)}"></label><p>Alleen nodig als je observaties wilt bewaren. Gebruik voor elk kind een andere code. De notities blijven in deze browser op deze iPad. Zonder code kun je alle opdrachten gewoon uitvoeren en observeren.</p><button data-export>Download observaties</button><p data-observation-status role="status"></p></details>`+goals.map(g=>{
  const tasks=missions.filter(available).flatMap(tasksFor).filter(t=>t.goals.includes(g.code));
  return `<article class="curriculum-goal"><h3>${g.code} · ${escapeHtml(g.description)}</h3><details><summary>Leerplan, opdrachten en waar je op let</summary><p>Bron: Doelenset BaO ICT.xlsx · ICT · rij ${g.row} · kolom ${state.age==='6-7'?'Q':'R'}.</p>${g.content.filter(c=>c.type?.startsWith('MIA')).map(c=>`<p>${escapeHtml(c.text)}</p>`).join('')}<p>Herhaal in een andere situatie. Begeleiding bij algoritmes en aangereikte zoektermen horen bij het leerplan.</p>${tasks.map(task=>`<p>${escapeHtml(task.prompt)}${task.criteria?'<br><em>'+escapeHtml(task.criteria)+'</em>':''} <button data-practice="${task.id}">Open opdracht</button></p>`).join('')}</details><details class="optional-record"><summary>Optioneel: een observatie noteren</summary><form data-observe="${g.code}"><label>Hoe lukte het?<select name="level"><option value="">Kies je observatie</option>${Object.entries(levels).map(([k,v])=>`<option value="${k}">${v}</option>`).join('')}</select></label><label>Wat zag je? In welke app? Welke hulp?<textarea name="context" maxlength="600" required></textarea></label><button>Observatie bewaren</button><p role="status"></p></form><div data-history="${g.code}"></div></details></article>`;
 }).join('');
 const history=()=>{
  const records=readObservations().filter(r=>r.learner===observationLearner&&r.age===state.age);
  t.querySelectorAll('[data-history]').forEach(el=>{el.innerHTML=records.filter(r=>r.goal===el.dataset.history).slice(-5).reverse().map(r=>`<p><strong>${escapeHtml(r.date.slice(0,10))} · ${escapeHtml(levels[r.level]||r.level)}</strong><br>${escapeHtml(r.context)}</p>`).join('')||'<p>Nog geen observaties voor deze leerlingcode.</p>'});
 };
 t.querySelector('#observationLearner').oninput=e=>{observationLearner=e.target.value.trim();history()};
 t.querySelectorAll('[data-observe]').forEach(form=>form.onsubmit=e=>{
  e.preventDefault();const message=form.querySelector('[role="status"]'),level=form.elements.level.value,context=form.elements.context.value.trim();
  if(!observationLearner||!level||!context){message.textContent='Vul leerlingcode, observatie en context in.';return;}
  const records=readObservations();records.push({learner:observationLearner,age:state.age,goal:form.dataset.observe,level,context,date:new Date().toISOString()});
  try{localStorage.setItem(observationKey,JSON.stringify(records));message.textContent='Observatie bewaard.';form.reset();history()}catch{message.textContent='Bewaren lukt niet. De observatie is niet opgeslagen.'}
 });
 t.querySelectorAll('[data-practice]').forEach(b=>b.onclick=()=>{
  const mission=missions.find(m=>m.tasks.some(task=>task.id===b.dataset.practice)),task=mission.tasks.find(task=>task.id===b.dataset.practice);
  t.close();session={mission,tasks:[task],index:0,correct:0,answered:false,singlePractice:true};$('#missionTitle').textContent=mission.title;$('#missionNumber').textContent='Gericht oefenen';renderTask();dialog.showModal();
 });
 t.querySelector('[data-export]').onclick=()=>{
  const rows=readObservations().filter(r=>r.learner===observationLearner&&r.age===state.age),status=t.querySelector('[data-observation-status]');
  if(!observationLearner||!rows.length){status.textContent='Kies een leerlingcode met bewaarde observaties.';return;}
  const blob=new Blob([JSON.stringify({learner:observationLearner,age:state.age,observations:rows},null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),link=document.createElement('a');
  link.href=url;link.download='ict-observaties-'+state.age+'.json';link.click();setTimeout(()=>URL.revokeObjectURL(url),1000);status.textContent='Download klaargezet voor deze leerlingcode.';
 };
 history();t.showModal();
};
$('#teacherBtn').onclick=()=>openTeacher();
// A single task opened by the teacher cannot complete an entire mission.
const finishCurriculumMission=finishMission;
finishMission=function(){
 if(!session.singlePractice)return finishCurriculumMission();
 body.innerHTML='<section class="work-panel"><h3>Opdracht afgerond</h3><p>Bespreek wat al lukt en wat je nog wilt oefenen.</p><button data-close>Terug naar de missies</button></section>';
 body.querySelector('[data-close]').onclick=()=>dialog.close();save();
};
const renderCurriculumDashboard=render;
render=function(){renderCurriculumDashboard();const tile=grid.querySelector('[data-id="systemen"]');if(tile){tile.querySelector('.status').textContent='Samen op de iPad';tile.querySelector('p').textContent='Maak en onderzoek met echte apps. Met voorleeshulp en afdrukbare kaarten.';tile.querySelector('.card-foot').textContent='Echte opdracht · leerkracht kijkt mee'}grid.querySelectorAll('.mission-card.done .status').forEach(el=>{if(el.closest('[data-id]').dataset.id!=='systemen')el.textContent='★ Route doorlopen'})};
if(state.age)render();

// Name and compose a real message instead of copying an exact model sentence.
renderFileEdit=function(task){
 const a=$('#taskArea');let savedName='',stage=0;
 a.innerHTML=`<section class="work-panel"><h3>Een bericht voor de klas</h3><p>${task.name==='klasuitstap'?'Vertel waar jullie naartoe gaan en wat een klasgenoot moet meenemen.':'Vertel wat je klasgenoot moet meenemen naar de kijktafel.'} Bedenk zelf je bericht en een naam waaraan je het herkent.</p><div class="editor"><label>Bestandsnaam<input class="typing-box" data-name maxlength="60" autocomplete="off"></label><label>Mijn bericht<textarea class="typing-box" data-text maxlength="1200"></textarea></label><button data-save>Bewaren</button><button data-close>Sluiten</button></div><div class="saved-file" hidden></div><p class="practice-note" role="status">Maak iets dat jouw klasgenoot kan gebruiken.</p></section>`;
 a.querySelector('[data-save]').onclick=()=>{
  if(session.answered)return;const name=a.querySelector('[data-name]').value.trim(),text=a.querySelector('[data-text]').value.trim();
  if(!name||!text)return note('Schrijf je bericht en geef het een herkenbare naam.');
  const work=readWork();if(work[name]&&name!==savedName)return note('Die naam bestaat al. Kies een andere naam zodat je het vorige werk bewaart.');
  try{saveWork(name,{kind:'text',text});savedName=name;stage=1;note('Bewaard. Sluit het bericht en zoek het daarna zelf terug.')}catch{note('Bewaren lukt niet. Je tekst staat nog hier. Vraag hulp.');}
 };
 a.querySelector('[data-close]').onclick=()=>{
  if(stage!==1)return note('Bewaar eerst je bericht.');
  if(a.querySelector('[data-name]').value.trim()!==savedName||a.querySelector('[data-text]').value.trim()!==readWork()[savedName]?.text)return note('Je veranderde iets. Bewaar opnieuw voordat je sluit.');
  stage=2;a.querySelector('.editor').hidden=true;const list=a.querySelector('.saved-file');list.hidden=false;
  list.innerHTML='<h3>Zoek jouw bericht terug</h3><p>Kies het bestand aan de naam.</p>'+Object.entries(readWork()).filter(([,item])=>item.kind==='text').map(([name])=>`<button data-file="${escapeHtml(name)}">${escapeHtml(name)}</button>`).join('')+'<div class="opened-message"></div>';
  list.querySelectorAll('[data-file]').forEach(b=>b.onclick=()=>{
   const name=b.dataset.file,item=readWork()[name];list.querySelector('.opened-message').textContent=item?.text||'';
   if(name!==savedName)return note('Dit is een ander bericht. Kijk naar de naam en zoek jouw nieuwe bericht.');
   if(stage!==2)return;stage=3;completePractice(task,'Je benoemde, bewaarde, sloot en heropende je eigen bericht. Laat je maatje lezen of de inhoud duidelijk is.');
  });
 };
};
missions.find(m=>m.id==='bestanden').tasks.filter(t=>t.type==='file-edit').forEach((t,i)=>{
 t.id='bestanden-eigen-bericht-v1-'+i;t.prompt=i?'Maak een eigen bericht over een klasuitstap. Geef het een herkenbare naam, bewaar het, sluit het en zoek het terug.':'Maak een eigen bericht voor de kijktafel. Kies zelf een herkenbare naam. Bewaar, sluit en heropen je bericht.';
});

// Default classroom use: no login and no persistent pupil progress.
let keepDeviceProgress=readJson('zisa-keep-device-progress',false)===true;
const loadSavedClassProgress=loadPracticeProgress,saveClassProgress=save;
loadPracticeProgress=function(){
 if(keepDeviceProgress)return loadSavedClassProgress();
 evidence={tasks:{},observed:[],done:[]};state.done=[];state.observed=[];state.hits={};
};
save=function(){if(keepDeviceProgress)saveClassProgress()};
const classroomTools=document.createElement('section');classroomTools.className='classroom-tools';
classroomTools.innerHTML=`<p><strong>Samen oefenen, zonder inloggen.</strong> Je leerkracht kijkt mee. Elk groepje kan opnieuw beginnen.</p><button data-new-group class="quiet-button">Nieuw groepje</button><details><summary>Voor de leerkracht: voortgang</summary><label><input type="checkbox" data-keep ${keepDeviceProgress?'checked':''}> Onthoud de oefenvoortgang op deze iPad</label><p>Standaard wordt de oefenvoortgang niet bewaard na herladen. Deze keuze gaat over missiesterren, niet over werkjes die je bewust bewaart. Op een gedeelde iPad horen sterren niet bij één kind.</p></details>`;
dashboard.append(classroomTools);
classroomTools.querySelector('[data-new-group]').onclick=()=>{observationLearner='';evidence={tasks:{},observed:[],done:[]};state.done=[];state.observed=[];state.hits={};save();render();toast('Klaar voor een nieuw groepje. Bewaarde werkjes blijven staan.');};
classroomTools.querySelector('[data-keep]').onchange=e=>{
 keepDeviceProgress=e.target.checked;
 try{localStorage.setItem('zisa-keep-device-progress',JSON.stringify(keepDeviceProgress));if(keepDeviceProgress)save();toast(keepDeviceProgress?'Voortgang wordt op deze iPad bewaard.':'Nieuwe oefenvoortgang wordt niet meer bewaard.');}catch{keepDeviceProgress=false;e.target.checked=false;toast('Bewaren is niet beschikbaar. Je kunt gewoon oefenen.');}
};
loadPracticeProgress();if(state.age)render();
